import { SkeletonCard } from '@/components/shared/SkeletonCard'

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <h1 className='ml-36'>Loading ROOT!!!</h1>
}
